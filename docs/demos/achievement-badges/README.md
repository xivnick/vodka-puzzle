# 대표 배지 홈 시안

기존 홈 화면에 가상 닉네임·기록·배지를 넣은 독립 시안이다. 인증·DB·완료 저장 연결은 없다.

`index.html`을 정적 서버로 열면 일반·데일리 순위를 전환하고 배지를 눌러 달성 조건을 볼 수 있다.

아이콘: [Tabler Icons v3.40.0](https://github.com/tabler/tabler-icons/tree/v3.40.0), MIT. 원본 SVG를 인라인으로 사용하며 색상·크기는 CSS로 지정한다. 라이선스는 `icons/LICENSE`에 보관한다.
