# 퍼즐 페이지 디자인 가이드

## 기본 방향
- **하얀 배경**, 플랫 디자인
- **차분한 레트로** 느낌 (부드러운 색감, 둥근 모서리 최소화)
- PC와 모바일 모두 대응하는 반응형 레이아웃

## 색상
| 용도 | 색상 |
|------|------|
| 배경 | `#ffffff` |
| 보드 셀 배경 | `#ffffff` |
| 보드 테두리 (굵은선) | `#222222` |
| 보드 테두리 (가는선) | `#aaaaaa` |
| 주어진 숫자 (given) | `#222222` |
| 사용자 입력 숫자 | `#4a6fa5` |
| 선택된 셀 배경 | `#e8e8d8` |
| 오류 셀 배경 | `#f5d0d0` |
| 오류 숫자 색상 | `#c0392b` |
| 숫자패드 배경 | `#f4f1eb` |
| 숫자패드 테두리 | `#c8c3b8` |
| 위험 버튼 배경 | `#fdf5f5` |
| 위험 버튼 테두리 | `#e0bfbf` |
| 위험 버튼 텍스트 | `#b05050` |
| 위험 버튼 호버 배경 | `#f5e0e0` |
| 본문 텍스트 | `#333333` |
| 보조 텍스트 | `#888888` |

## 타이포그래피
- **모든 텍스트**: `'Noto Sans KR', sans-serif` (Google Fonts)
- 제목: Noto Sans KR 700
- 보드 숫자: Noto Sans KR 700
- UI 텍스트/버튼: Noto Sans KR 400~500

## 레이아웃
- 보드는 화면 중앙 정렬
- PC: 보드 최대 `450px`, 여유 있는 패딩
- 모바일: 보드 `94vw` 이하, 숫자패드도 화면 너비에 맞춤

## 버튼 레이아웃
- 숫자패드(1~9)와 지우기 버튼은 보드와 동일한 너비로 정렬
- 숫자 버튼 9개는 균등 분배 (`flex: 1`), 동일한 높이
- 지우기 버튼은 숫자패드 아래 별도 행, 보드 전체 너비로 배치
- 초기화 버튼도 보드 전체 너비로 배치
- 버튼 간 간격은 `6px` (모바일 `5px`)
- 모든 버튼의 높이 통일 (`44px`, 모바일에서는 vw 기반 스케일)

## 보드 스타일
- 3×3 박스 구분선: 2.5px 굵은 실선
- 셀 구분선: 1px 가는 실선
- 그림자, 그라데이션 없음 (플랫)
- 셀 모서리 둥글기 없음 (각진 그리드)

## 위험 버튼 (초기화 등)
- 되돌리기 어려운 동작에는 `.danger` 클래스를 적용
- 연한 붉은 톤 배경(`#fdf5f5`), 붉은 테두리(`#e0bfbf`), 붉은 텍스트(`#b05050`)
- 호버 시 약간 더 진한 붉은 배경(`#f5e0e0`)
- 과하지 않고 차분하게, 주의가 필요하다는 정도만 표현

## 인터랙션
- 셀 선택 시 배경색 변경 (부드러운 베이지/크림 톤)
- 규칙 위반 셀은 연한 붉은 배경 + 붉은 숫자
- 호버 효과는 최소한으로 (배경 미세 변화)
- 완성 시 축하 메시지 표시

## 반응형 기준
- 모바일 (`max-width: 540px`): 보드와 패드가 화면 폭에 맞춤
- PC: 고정 최대 너비, 중앙 정렬
